/**
 *
 * @param userName 用户名
 * @returns {用户签名}
 */
function getUserDiscribe(userName) {
  var discribe = null
  return discribe
}

/**
 *
 * @param userID 用户ID
 * @returns {用户名}
 */
function getUserName(userID) {
  var username = null
  return username
}
